$Id$

Provide custom text with tokens where fallback tokens can be can be used if a
token is empty.

Example:

[field_image_1]
[field_image_2]
[field_image_3]

If [field_image_1] was empty [field_image_2] will be rendered and the following
tokens will be ignored.